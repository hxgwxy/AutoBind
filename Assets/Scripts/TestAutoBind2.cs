using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public partial class TestAutoBind2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var a = Image_Img;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
